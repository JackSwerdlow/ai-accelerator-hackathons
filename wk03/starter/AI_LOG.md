# AI Assistance Log

Record every instance where you use AI to generate, refactor, or debug code. Four fields per entry: what you asked for, what the AI produced, what you changed, and why you changed it. There is an example provided of how the format for each should look.

## Example: Prompt 0: Eligibility logic for married couple's allowance edge case

| Field | Detail |
|-------|--------|
| **Date + Time** | YYYY-MM-DD HH-MM-SS |
| **Task** | Scaffolded eligibility check for married couple's allowance |
| **What AI Generated** | if/else chain covering 5 scenarios: single owner, joint owner, private renter, housing association renter, and council tenant. Used hardcoded age threshold of 65 for pension-credit eligibility. Did not include civil-partnership status. Error message on ineligible path was generic ("You are not eligible"). |
| **What You Changed + Why** | Removed hardcoded age threshold (was 65, should be state-pension-age variable) -- hardcoded age breaks if policy changes. Added missing civil-partnership case alongside married status -- civil partnership is a legal requirement since 2004. Changed error message from generic to specific ("You are not eligible because your household income exceeds the threshold") -- generic errors do not help users correct their input. |

## Prompt 1: [Agent-Jack] Content plan for the Green Home Grant eligibility checker

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Create a content-first plan covering all page copy, question options, eligibility rules, and result messages for the service — saved to `docs/plans/2026-06-03-content-plan.md` |
| **What AI Generated** | Full content plan including: start page description text; exact question headings, radio option labels and values, hint text, and error messages for all 5 question pages; check-your-answers row labels and display-label mapping; 5 priority-ordered eligibility rules in plain language; measures-available logic per property type; result page panel titles and body paragraphs for all three outcomes (eligible, partial, ineligible) including sub-cases for renter vs mid-income and income-too-high vs already-fitted; GOV.UK error pattern description; accessibility statement and footer link content; and a content decisions rationale table. |
| **What You Changed + Why** | No changes made to the generated content — this was a planning artefact reviewed before any code was written. Content decisions (e.g. three income bands rather than free-text, all renters routed to partial regardless of income) were accepted as reasonable for a fictional scheme. Grant amounts ("up to £10,000 / £5,000") accepted as appropriately hedged. |

## Prompt 2: [Agent-Jack] Agent work split plan for 4-way parallel development

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Define how to split the Green Home Grant build across 4 agents to maximise parallel work and minimise merge conflicts — saved to `docs/plans/2026-06-03-agent-work-split.md` |
| **What AI Generated** | Work split assigning strict file ownership to each agent: Agent 1 (App.jsx + all shared components + Start/Accessibility pages), Agent 2 (question pages 1–3), Agent 3 (questions 4–5 + Check Answers), Agent 4 (eligibility logic + Result page + unit tests). Included an agreed interface contract (formData shape and prop signature), a full file ownership table, conflict risk analysis, and notes for collating all four agents' plans into one implementation plan. |
| **What You Changed + Why** | No changes — reviewed and accepted as a planning artefact. Strict file ownership confirmed as the right strategy; AI_LOG.md is the only unavoidable shared file and is low-risk to merge. |

## Prompt 3: [Agent-Research] Standards research report for Green Home Grant

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Research the standards this service must conform to (GOV.UK Design System, WCAG 2.2 AA, GDS Service Standard, PSBAR, content style) and produce `docs/research/research.md` with primary-source links and direction for later requirements / solution planning. Sanity-check the report by spawning subagents. |
| **What AI Generated** | `docs/research/research.md` — ten numbered sections covering: how to read the doc, legal baseline (PSBAR + Equality Act + UK GDPR + PECR), GDS Service Standard with relevance rating per point, WCAG 2.2 AA (POUR, load-bearing AA SCs for forms, new-in-2.2 criteria most likely to bite this service), GOV.UK Design System (patterns, components, pattern-specific rules including question pages, check answers, confirmation, validation, phase banner), content design (style guide and form-structure guidance), accessibility statement structure, cross-browser/mobile expectations, adjacent technical standards (security headers, performance, analytics, inclusive language), testing approach, and 11 directional questions for the next phase. Two appendices: quick-reference link list and explicit out-of-scope items. |
| **What You Changed + Why** | After three sanity-check subagents reviewed the draft, made these factual / structural corrections: (1) relabelled WCAG 3.3.7 Redundant Entry and 3.2.6 Consistent Help from AA to A (they are new in 2.2 but at Level A); (2) reconciled accessibility-statement section count from 7 to the actual 8 in the GOV.UK model; (3) added missing coverage of UK GDPR / PECR / cookies, the "Check if a service is suitable" eligibility-screening pattern (a major omission for an eligibility checker), Service Assessment process, security headers, performance, analytics, inclusive language; (4) added 1.4.4 Resize Text to the load-bearing SC table to match a forward reference in §9; (5) softened overstated claims about the check-answers title and the "Start using a service" replacement history; (6) expanded §10 from 6 to 11 directional items to cover cookie posture, eligibility-test strategy, browser-support floor, Welsh-language scoping, and the phase-banner feedback link target; (7) added Appendix B listing intentional out-of-scope items (AI_LOG, stretch challenges, i18n, Prototype Kit) for traceability. |

## Prompt 4: [Agent-Jack] Full implementation plan for the Green Home Grant eligibility checker

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Create a comprehensive implementation plan drawing together content plan, standards research, and frontend-design skill guidance — saved to `docs/plans/2026-06-03-implementation-plan-jack.md` |
| **What AI Generated** | Full implementation plan covering: design philosophy (refined polish within strict GOV.UK constraints); new file list (`progress.css`, `ProgressIndicator.jsx`, eligibility utils); agreed interface contract (formData shape + eligibility function signature); task checklist with checkboxes organised by feature (infrastructure, start page, five question pages, check-answers, eligibility logic, result page, accessibility statement, page transitions); three specific elevation choices (progress indicator, 150ms page-transition CSS animation, inline SVG icons on result panels); per-task GOV.UK pattern requirements (legend wrapping, hint text, error summary link targets, aria-describedby); GOV.UK compliance checklist; test strategy table; definition-of-done criteria. |
| **What You Changed + Why** | Removed per-agent task assignment from the plan structure (user asked to ignore agent-work-spec); reorganised by feature/component instead. Resolved tension between frontend-design skill's bold-aesthetic direction and the GOV.UK compliance rubric by explicitly naming the constraint and limiting elevation to three low-risk additions that are within GOV.UK standards. Added `prefers-reduced-motion` guard on the page-transition animation to preserve accessibility. |

## Prompt 5: [Agent-SK] Parallel implementation plan + coding-standards section for the Green Home Grant eligibility checker

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Create an implementation plan to be executed by four agents working in parallel, saved alongside the content plan. The plan was iterated in the same session to add (a) a unique-author suffix in the filename so multiple authors writing implementation plans to the same folder don't clash; (b) a coding-standards section covering state-management rules, React hook rules, a dedicated routes file, naming conventions, comments/JSDoc rules, responsive-design breakpoints, and a per-agent change-log convention; (c) a contextual-help / tooltip rule mapped to GOV.UK-compliant patterns. Drafted before pulling latest, so done in parallel with Agent-Jack's own work-split and implementation plans rather than in coordination with them. Per user instruction, neither plan replaces the other — both kept side-by-side, reconciliation deferred. |
| **What AI Generated** | `docs/plans/2026-06-03-implementation-plan-Agent-SK.md` — full plan (~1350 lines). Sections: (1) purpose and references; (2) architecture decisions; (3) shared contracts naming the form-state shape, a `useFormContext()` hook (React Context, not prop drilling), an `eligibility(answers)` function returning `{ outcome, reason, measures }` where the `reason` code (`owner-low-income`, `owner-mid-income`, `renter`, `income-too-high`, `no-measures-needed`, `default`) drives Result-page copy variants per content plan §7, and a `<QuestionPage>` props contract abstracting the five radio question pages; (3a) coding-standards section covering: state-management rules (no Redux/Zustand/etc., context only, derive don't store), React hook rules (top-level only, allow-list of hooks), dedicated `src/router.jsx`, per-agent change-log convention at `wk03/docs/agents/Agent-<id>-changes.md`, responsive design at 320/640/769+ breakpoints with WCAG 2.5.8 tap-target enforcement, naming conventions (file/var/component/boolean/handler/ref/class), comments and JSDoc rules (header per file, JSDoc on exports, no commented-out code in main), and a three-tier contextual-help rule — Tier 1 visible hint text via QuestionPage `hint` prop; Tier 2 `<details>`/`<summary>` "Help with this question" required on Income/Insulation/Heating; Tier 3 accessible tooltip only on non-form supplementary UI, never on form inputs, with keyboard/focus/Esc/aria-describedby requirements; (4) file ownership map naming exactly one owner per file; (5) pre-flight; (6) reference HTML patterns; (7) four lane assignments — Lane A foundation + chrome + Start + Accessibility Statement; Lane B reusable QuestionPage + Property/Ownership/Income; Lane C Insulation + Heating + CheckAnswers + Result; Lane D eligibility + tests + test infra — each with 6–11 small tasks specifying files, acceptance criteria, and suggested commit messages; (8) cross-lane sequencing diagram identifying critical-path tasks (A2 FormContext, B2 QuestionPage, D2 eligibility); (9) integration checklist (functional, accessibility, responsive, standards, logs); (10) commit/branch workflow; (11) definition of done. |
| **What You Changed + Why** | (1) Renamed plan file to include `-Agent-SK` suffix so it doesn't collide with other authors writing implementation plans on the same date — matches the `[Agent-Jack]` author convention already used in commits. (2) Added §3a coding-standards section after the initial draft because the first version assumed conventions implicitly and downstream agents need explicit rules. (3) Mapped the user's request for "tooltips" to GOV.UK-compliant patterns (hint text, `<details>` block) rather than implementing literal hover tooltips, because GOV.UK avoids hover-only tooltips for accessibility reasons (touch, keyboard, screen reader). Tier 3 tooltips allowed only on non-form supplementary UI with strict a11y constraints. (4) Added per-agent change-log convention separate from AI_LOG.md so each agent has an append-only namespaced file recording what code they changed (zero merge conflicts) — AI_LOG remains for prompt records. (5) Added Task A2.5 (extract routes to `src/router.jsx`) so the route table is grep-able and reviewers see all paths in one place. (6) Expanded each lane's self-QA task and §9 integration checklist with explicit 320px and 640px responsive checks. (7) Per user instruction after `git pull` revealed conflicts: did NOT replace, edit, or delete Agent-Jack's `2026-06-03-implementation-plan-jack.md`, `2026-06-03-agent-work-split.md`, or `research.md`. This plan is kept as a parallel/alternative. **Material differences with Agent-Jack's plan that the next reader should know about before executing**: (i) state management — mine uses React Context with `useFormContext()`, Jack uses prop drilling with `formData`/`setFormData`; (ii) eligibility return shape — mine returns `{ outcome, reason, measures }` with `reason` driving Result-page copy variants per content plan §7, Jack returns `{ result, measures }` and would need to recompute the reason in the Result page; (iii) question pages — mine abstracts a `<QuestionPage>` component used by all five, Jack implements the radio pattern per page; (iv) routes — mine in `src/router.jsx`, Jack inline in `App.jsx`; (v) `App.css` — Jack says do-not-modify, mine extends with missing classes (phase banner, footer, `<details>`, skip link, hint, link, grid helpers); (vi) mine has coding-standards, tooltip-rule, responsive-checks, and per-agent change-log conventions that Jack's plan does not. Both plans are end-to-end executable but cannot be executed simultaneously — reconciliation deferred per user instruction. |

## Prompt 6: [Agent-Jack] Collated single implementation plan (PLAN.md) from all four prior plan docs

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Read all five prior docs in `wk03/docs/` (content plan, two implementation plans, two research files), surface agreements and conflicts to the user for decisions, then collate the agreed approach into a single agent-agnostic `wk03/docs/PLAN.md` that subsequent agents will follow to build the webapp. Run the work via a structured prompt: invoke `superpowers:writing-plans`, verify library APIs via Context7, draft the full plan, dispatch two parallel review agents (path/symbol verification + GOV.UK Design System fidelity), apply fixes, call `advisor()`, then commit. Ignore agent-split planning at this stage. |
| **What AI Generated** | `wk03/docs/PLAN.md` — 15 sections covering: goal and scope; references table; 17 numbered architecture decisions (AD1–AD17) each with rationale and source plan; shared contracts including the exact `Answers` shape, `useFormContext()` signature, `eligibility(answers)` return shape with a 6-row reason-code table, `<QuestionPage>` prop signature, and the 9-row routes table; coding standards (state, hooks, focus discipline, naming, comments); reference patterns as full copy-paste templates (QuestionPage HTML, ErrorSummary component, document.title `useEffect`, focus-on-route-change wired into App.jsx, SummaryList markup, ResultPage panel, `<details>` help block, SkipLink, ProgressIndicator); flat files-to-create-or-modify list (no lane assignments); a load-bearing "GOV.UK Styling Discipline" section enumerating what's enforced, what's forbidden, and the App.css classes to add; three permitted polish enhancements within Design System bounds (`app-step-indicator`, 150ms motion-safe page-enter transition, `<details>` help on Income/Insulation/Heating); a sessionStorage persistence stretch sketch; a WCAG 2.2 AA checklist grouped by POUR citing SC numbers (1.1.1, 1.3.1, 1.3.5, 1.4.3, 1.4.4, 1.4.5, 1.4.10, 1.4.11, 1.4.12, 2.1.1, 2.4.1, 2.4.2, 2.4.3, 2.4.4, 2.4.7, 2.4.11, 2.5.3, 2.5.7, 2.5.8, 3.2.6, 3.3.1, 3.3.2, 3.3.3, 3.3.7, 3.3.8, 4.1.2, 4.1.3) with N/A reasoning where load-bearing; full test strategy (Vitest + RTL setup with exact package.json/vite.config.js diffs, 13-row eligibility test matrix with reason-code coverage including edge cases, 5-row QuestionPage component test matrix using MemoryRouter + FormProvider, manual test plan); Definition of Done mapped to README acceptance criteria; explicit non-goals list (state libraries, react-hook-form, Zod, TanStack, TypeScript, govuk-frontend npm, panel SVG icons, hover tooltips, per-agent change-logs, lane assignments); open questions for the user (phase-banner target, "Start again" link, Welsh language, cookie posture, dev-server hostname). Context7 calls verified the react-router-dom v6.20 hook syntax (`useNavigate`, `useSearchParams`, `MemoryRouter`), the Vitest `test` block format in `vite.config.js`, and the @testing-library/react `render({ wrapper })` pattern before any code snippet was written. |
| **What You Changed + Why** | Before drafting, ran a multi-turn synthesis with the user to surface agreements vs disagreements across the five source docs and let the user pick between competing approaches (e.g. Context vs prop drilling, generic QuestionPage vs per-page implementation, panel icons vs no icons). User selected the recommended approach across all decisions. **Process compliance issue I caught via `advisor()` after drafting**: I silently removed AD8 (focus to `<h1>` on route change) when the GOV.UK-fidelity reviewer flagged that focusing an H1 inside a `<legend>` causes NVDA/JAWS to double-announce. The user's prompt had listed AD8 under "do not change without flagging". I surfaced it to the user with three options; user delegated the technical choice with "pick whatever is best practice and inline with GOV.UK styling and accessibility". Resolved AD8 to "focus `<main id='main-content'>` on route change" — the pattern used by live GOV.UK services (no legend-nesting issue, no extra screen-reader announcement noise) — and updated §6.4 with the inline App.jsx hook pattern. **Other fixes from reviews**: (1) §6.1 DOM order corrected — hint then inline error inside the fieldset; `<details>` moved OUTSIDE the fieldset (supplementary content, not part of the field); (2) §6.2 ErrorSummary removed `role="alert"` — canonical govuk-frontend pattern focuses the container instead, role+focus together can cause double-announcement; (3) §6.6 result panel — added `govuk-panel--confirmation` modifier for eligible/partial and documented that `govuk-panel--not-eligible` is a teaching-scaffold extension, not a Design System token; (4) §6.9 renamed `govuk-step-indicator` to `app-step-indicator` because the `govuk-` prefix should only be used for Design System tokens and this is a custom component; (5) §6.1 back link changed from `<a>` to `<Link>` (and same for check-answers Change link in §6.5) — plain anchors trigger full-page reload, wiping FormContext state; (6) §8.1 pinned phase-banner dash character to en dash (U+2013) matching content plan §1, so implementers don't normalise it; (7) §8.2 added "decorative SVGs must carry both `aria-hidden='true'` and `focusable='false'`"; (8) §11 added WCAG 2.4.4 (link purpose) and 2.5.3 (label in name) as load-bearing, and added explicit N/A entries for 1.3.5, 1.4.5, 2.5.7, 3.3.8 with reasoning; (9) §13 tightened loose acceptance items (exact `document.title` format, step-indicator placement constraint, accessibility-statement 8-section completeness, no-`localhost` URL check, content quality on `<details>` blocks). The parallel path-verification agent confirmed every path in §7 is correctly classified (Modify files exist on disk, Create files do not), every CSS variable referenced is defined in App.css, and every package version in the test infra is compatible with React 18 + Vite 5. No mismatches required code changes. |

## Prompt 7: [Agent-Jack] Chunk 1 — Test infrastructure + shared contracts (eligibility, FormContext, displayLabels) + eligibility tests

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Implement Chunk 1 of `wk03/docs/PLAN.md`: add Vitest + React Testing Library test infrastructure, then create the three shared-contract modules (pure `eligibility(answers)`, `FormContext` with `useFormContext` hook, `displayLabels` mapping table) plus a comprehensive `eligibility.test.js` covering all rule branches, measures conditions, and robustness cases. Run end-to-end via `superpowers:subagent-driven-development`: dispatch a fresh implementer subagent with the full chunk spec, then a fresh spec-compliance reviewer, then a fresh code-quality reviewer; address review findings before committing. |
| **What AI Generated** | Implementer subagent created/modified 7 files: (1) `package.json` — added `test`/`test:run` scripts and the 5 devDependencies (`@testing-library/jest-dom@^6.6.0`, `@testing-library/react@^16.1.0`, `@testing-library/user-event@^14.5.2`, `jsdom@^25.0.0`, `vitest@^2.1.0`) leaving existing react/react-dom/react-router-dom intact; (2) `vite.config.js` — added `/// <reference types="vitest" />` and the `test` block (`globals: true, environment: 'jsdom', setupFiles: ['./src/__tests__/setup.js'], css: false`) while preserving the existing host/port/hmr server config; (3) `src/__tests__/setup.js` — RTL jest-dom matchers import + `afterEach(cleanup)`; (4) `src/eligibility.js` — pure named export with defensive `answers ?? {}` guard, all 5 rules in priority order via early returns with a default fallback, internal `computeMeasures()` helper computing measures independently of outcome using `!== 'full'` / `!== 'flat'` / `!== 'heat-pump'` semantics; (5) `src/contexts/FormContext.jsx` — `FormProvider` + `useFormContext` (throws if used outside provider) + exported `INITIAL_ANSWERS`; (6) `src/displayLabels.js` — `labelFor(field, value)` covering all 5 fields × 22 value mappings character-for-character from content plan §4 (returns `""` for empty/unknown), `measuresFor(answers)` delegating to `eligibility().measures` for single source of truth; (7) `src/__tests__/eligibility.test.js` — 14 tests across three describe blocks (eligibility/measures/robustness) covering rules 1–5 + default + flat/full-insulation/heat-pump measure exclusions + 3 robustness cases (`{}`, unknown value, no-arg). All 14 tests passed first run; `npm install` succeeded (109 packages, expected vulnerability/deprecation warnings, no errors). |
| **What You Changed + Why** | Followed the full subagent-driven-development workflow: implementer → spec reviewer (✅ all 7 files match spec character-for-character, rules in correct priority order, no missing/extra exports) → code-quality reviewer (APPROVED with two non-blocking Important findings). Made one code-quality fix before committing: added tests 8a and 8b to `eligibility.test.js` to genuinely demonstrate rule priority (8a: high income + full insulation + heat pump returns `income-too-high` not `no-measures-needed`, proving rule 1 beats rule 2; 8b: private-renter + low income returns `renter` not `owner-low-income`, proving rule 3 beats rule 5). Without these, a future regression that re-ordered rules could still pass the rule-1 test. Skipped the reviewer's other Important finding (distinguish empty-value vs unknown-field returns in `labelFor`) because both are safe for the only call site (CheckAnswers/Result) and a console.warn would add noise for legitimate empty-state rendering during the user journey. Skipped all Minor findings (param naming `a` vs `answers` in private helper, JSDoc line-length, separate `export {}` vs inline) as not affecting correctness or maintainability. Final test count: 16/16 passing. |

## Prompt 8: [Agent-Jack] Chunk 2 — Shared component library (SkipLink, ProgressIndicator, ErrorSummary, Panel, SummaryList, QuestionPage) + QuestionPage tests

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Implement Chunk 2 of `wk03/docs/PLAN.md`: the shared component library used by both App chrome (Chunk 3) and the page-level wrappers (Chunk 4). Specifically: `SkipLink` (§6.8 — plain `<a>`, NOT a router Link), `ProgressIndicator` (§6.9 — custom `app-step-indicator` class, NOT a Design-System token), `ErrorSummary` (§6.2 — auto-focuses on mount via `tabIndex={-1}` + `useRef`/`useEffect`, deliberately NO `role="alert"`), `Panel` (§6.6 — switches between `govuk-panel--not-eligible` and `govuk-panel--confirmation`, no SVGs/interactive content), `SummaryList` (§6.5 — uses react-router `<Link>` for Change links to preserve FormContext), `QuestionPage` (§4.4 + §6.1 — composed question screen with back link, progress indicator, error summary, fieldset/legend-as-H1, hint, inline error, radios, optional `<details>` help outside the fieldset, Continue button; `?from=check-answers` query overrides destination; `document.title` set via useEffect with `Error: ` prefix). Plus `QuestionPage.test.jsx` covering five test scenarios from PLAN.md §12.3. Driven end-to-end via `superpowers:subagent-driven-development` (implementer → spec review → code-quality review → fix → commit). |
| **What AI Generated** | Implementer subagent created 7 files: `src/components/SkipLink.jsx`, `src/components/ProgressIndicator.jsx`, `src/components/ErrorSummary.jsx`, `src/components/Panel.jsx`, `src/components/SummaryList.jsx`, `src/components/QuestionPage.jsx`, `src/__tests__/QuestionPage.test.jsx`. The QuestionPage matches §6.1 DOM order exactly (back link → progress → error summary → fieldset[legend(H1) → hint → inline error → radios] → details (outside fieldset) → Continue). `aria-describedby` is conditionally spread via a `fieldsetProps` object so the attribute is omitted entirely (not emitted as `""` or `undefined`) when neither hint nor error is present — load-bearing for assistive-tech behaviour. `handleContinue` reads `searchParams.get('from')` and navigates to `/check-answers` when it equals `'check-answers'`, otherwise to `onContinueNavigateTo`. Test file uses MemoryRouter + FormProvider via a `renderWithProviders` helper and a `LocationProbe` sibling component (`useLocation().pathname` rendered into a `data-testid="location"` div) to assert the post-Continue route. Initial run: 21/21 passing. |
| **What You Changed + Why** | Two changes before committing. (1) **Reworked test 4 to genuinely verify pre-check from existing context**, not click-then-check. The original test started with empty context, clicked a radio, then asserted it was checked — which only re-tests the onChange binding and does not exercise the spec scenario "when `answers[fieldName]` is non-empty, the matching radio is pre-checked on mount". The fix uses a small `<Seed field value />` component that calls `setAnswer` once on mount to seed the FormContext, then mounts the QuestionPage inside the same provider and asserts the matching radio is already checked with no user interaction. Initial fix caused a vitest hang because `setAnswer` is not memoised in `FormProvider`, so a `useEffect` with `[field, value, setAnswer]` deps re-fired every render of the provider and looped infinitely; resolved by switching the Seed's effect to empty deps with a one-line comment naming the constraint (`setAnswer is not memoised in FormProvider`). (2) Skipped the reviewer's other Important finding (hardcoded `id="error-summary-title"` in ErrorSummary) — fine for this flow because only one ErrorSummary is ever mounted, and adding a useId would add complexity for no real benefit. Skipped all Minor findings (line-length nits in JSDoc, file header verbatim from §6.2 being slightly over 3 lines, `fieldsetProps` indirection vs inline `aria-describedby={x || undefined}`) as not affecting correctness or maintainability. Note on the React Router v6→v7 future-flag warnings emitted during tests: informational only, do not affect test results; PLAN.md does not require setting the flags. Final test count: 21/21 passing (16 eligibility + 5 QuestionPage). |

## Prompt 9: [Agent-Jack] Chunk 3 — Chrome (Header/Footer/Button/PhaseBanner) + App.jsx wiring + router.jsx + App.css extensions

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Implement Chunk 3 of `wk03/docs/PLAN.md`: wire up the persistent app chrome, replace `App.jsx` to mount the FormProvider + SkipLink + Header + PhaseBanner + AppRoutes + Footer with focus-on-route-change per §6.4, extract the 9-route table to `src/router.jsx` per §4.5, and extend `App.css` with all §8.3 classes (skip link, app-step-indicator, phase banner + tag, footer, fieldset legend large + heading, hint, link, details, error-summary body, button start-icon, header container/service-name, motion-safe page-enter animation with `prefers-reduced-motion: reduce` guard, `.govuk-main-wrapper:focus { outline: none }`). Driven via `superpowers:subagent-driven-development`. |
| **What AI Generated** | Implementer subagent modified 6 files and created 1. (1) `src/App.jsx` — full rewrite: `useRef` + `useEffect` focuses `<main ref={mainRef} id="main-content" tabIndex={-1} key={pathname}>` on every pathname change; `key={pathname}` re-mounts main so the page-enter animation fires per route; FormProvider wraps the whole tree; chrome order matches §6.4. (2) `src/router.jsx` — created; default export `AppRoutes` returning a `<Routes>` table of exactly 9 routes. (3) `src/components/GovukHeader.jsx` — service-name `<Link to="/">Green Home Grant</Link>` added next to the GOV.UK wordmark; wordmark is a plain `<a>` (cross-government link). (4) `src/components/GovukButton.jsx` — `variant="start"` now renders a chevron SVG with both `aria-hidden="true"` AND `focusable="false"` (PLAN.md §8.2). (5) `src/components/PhaseBanner.jsx` — implementer caught and fixed a copy mismatch ("us improve it." → "us to improve it." matching content plan §1); en dash U+2013 byte-verified. (6) `src/components/GovukFooter.jsx` — `Accessibility statement` link converted from `<a>` to react-router `<Link>` so FormContext survives the click; `Cookies` link added per content plan §10. (7) `src/App.css` — appended ~170 lines of new GOV.UK and app-prefixed rules; all values reference existing custom properties from `:root`; the page-enter keyframe is namespaced `app-page-enter` and guarded by `prefers-reduced-motion`. Tests still pass 21/21; `npm run build` exits 0 (49 modules, 168 kB JS). |
| **What You Changed + Why** | Followed the full subagent-driven-development workflow: implementer → spec reviewer (✅ all 7 files compliant, en dash verified, 9 routes verified) → code-quality reviewer (APPROVED with two Important findings). Made one code-quality fix before committing: changed the GOV.UK wordmark `<a href="/">` to `<a href="https://www.gov.uk/">` in `GovukHeader.jsx`. The original `/` would have triggered a full-page reload back to the service Start page on click (wiping FormContext) — contradicting the file's own JSDoc rationale that "gov.uk is a separate site". Pointing to `https://www.gov.uk/` matches the canonical govuk-frontend behaviour and avoids the FormContext-loss footgun. Deferred the reviewer's other Important finding (page placeholders in `src/pages/*` still render their own `<main id="main-content">`, creating nested `<main>` and duplicate IDs) to Chunk 4 — every placeholder gets replaced wholesale in Chunk 4 so a Chunk-3 fix would be wasted work. Skipped all Minor findings (line-length on the PhaseBanner span — at 127 chars but intentionally single-string JSX to avoid whitespace-handling pitfalls; dead `.govuk-phase-banner .govuk-width-container` selector — harmless; keyframe name `app-page-enter` vs spec's literal `page-enter` — `app-` prefix is internally consistent with `.app-step-indicator`; double-vs-single quote inconsistency between chunks — not a behaviour concern). Final state: 21/21 tests pass, `npm run build` exits 0. |

## Prompt 10: [Agent-Jack] Chunk 4 — All 9 pages (Start, 5 questions, CheckAnswers, Result, AccessibilityStatement)

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Implement the final implementation chunk of `wk03/docs/PLAN.md`: replace all 9 page placeholders wholesale with full implementations following the content plan §2–§9 verbatim copy. Per AD10 in PLAN.md, CheckAnswers redirects to the first unanswered question if any answer is empty; Result redirects to `/` if any answer is empty. Every page wrapper renders fragments — App.jsx already provides the single `<main id="main-content">`, so pages must NOT render their own `<main>`. Driven via `superpowers:subagent-driven-development`. |
| **What AI Generated** | Implementer subagent replaced all 9 page placeholders and appended one CSS rule. (1) `src/pages/StartPage.jsx` — GOV.UK start-page pattern: H1, two body paragraphs, intro line, 5-bullet "you will need to know" list, `GovukButton variant="start"` to `/property-type`. (2-6) The five question pages (`PropertyTypePage`, `OwnershipPage`, `IncomePage`, `InsulationPage`, `HeatingPage`) — all thin `<QuestionPage>` wrappers with options/hint/helpDetails/errorMessage/back/continue per content plan §3 Q1-Q5. Income/Insulation/Heating have `<details>` "Help with this question" blocks per AD14. (7) `src/pages/CheckAnswersPage.jsx` — `useFormContext` reads answers, `useEffect` guard finds first unanswered field and `navigate(route, { replace: true })`, `<SummaryList rows={...}>` with `?from=check-answers` query strings on Change links, "Submit and see result" button to `/result`. (8) `src/pages/ResultPage.jsx` — calls `eligibility(answers)`, renders `<Panel outcome title body>` then branches body copy on `(outcome, reason)` for all 6 cases (eligible, partial/renter, partial/owner-mid-income, ineligible/income-too-high, ineligible/no-measures-needed, ineligible/default); guard redirects to `/` if any answer empty. (9) `src/pages/AccessibilityStatementPage.jsx` — all 8 PSBAR sections from research §7 plus the optional "What we are doing to improve accessibility" section, populated with content plan §9 values (Service name, partially compliant with WCAG 2.2 AA, prep/review date 3 June 2026, contact email accessibility@greengrant.gov.uk). (10) `src/App.css` — appended `.govuk-list--bullet { list-style: disc; }` (existing `.govuk-list` already supplies padding-left). Every new file has a 1-3 line header + JSDoc on the default export. 21/21 tests still pass; `npm run build` exits 0 (49 modules, 186 kB JS). |
| **What You Changed + Why** | Followed the full subagent-driven-development workflow: implementer → spec reviewer (✅ verbatim copy match for all 9 pages, £ symbols verified, both guards use `replace: true`, no nested `<main>` anywhere) → code-quality reviewer (APPROVED with three Important findings, all non-blocking). Skipped all three: (1) defensive fallback for `ineligible/default` and `partial/<other reason>` — both branches are unreachable given the eligibility rules in `eligibility.js`, and the brief render-flash window is bounded by the useEffect-based guards already in place; adding extra copy would be speculative; (2) extracting the title suffix `" - Green Home Grant - GOV.UK"` to a shared constant — five copies across five files is below the threshold where DRY pays for itself, and a constants module would create cross-chunk coupling for no behaviour benefit; (3) collapsing `FIELD_ORDER` + `ROW_LABELS` in CheckAnswersPage into one array-of-objects — two parallel maps keyed by the same field names are easy enough to read for five entries. All Minor findings (section comment in App.css, line-length on prose strings, parenthetical label duplication on the heating page) skipped as either subjective or covered by the "long content strings exempt" rule in PLAN.md §5. Final state: 21/21 tests pass, `npm run build` exits 0, no `<main>` in any page (verified via grep). |

## Prompt 11: [Agent-Jack] Final verification — end-to-end browser smoke test via Playwright MCP + vite.config.js TS-reference cleanup

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Verify the assembled service end-to-end before declaring done: re-run `npm run test:run` (21/21) and `npm run build` (exit 0), then drive the running dev server through the user journey in a real browser via Playwright MCP to confirm the wiring works in practice — not just in tests. Tests verify code correctness; only a browser run verifies feature correctness per project-level CLAUDE.md. |
| **What AI Generated** | Full smoke test against `http://lab14102.labs.decoded.com:5002`. Verified: (1) Start page renders title, description, 5-bullet list, and `Start now` button with chevron SVG; clicking navigates to `/property-type`. (2) Question page shows back link, "Step 1 of 5" progress indicator, fieldset with H1 inside legend, all 5 radio options, Continue button — and `main` is the active focus element. (3) Validation: clicking Continue with no selection sets `document.title` to `"Error: …"`, renders the error summary "There is a problem" with link `#propertyType-1`, sets the error summary as `document.activeElement`, and renders the inline error message with the visually-hidden "Error:" prefix above the radios — all per §6.1 and §6.2. (4) Happy path (owner + low + none + gas-boiler): reaches `/check-answers`, all 5 SummaryList rows render with displayed labels ("Detached house", "I own my home", "Under £31,000", "No insulation", "Gas boiler"), every Change link's href is `${route}?from=check-answers` with the visually-hidden field name appended. (5) Eligible result: panel title "You may be eligible for a Green Home Grant" with body "You may qualify for a grant of up to £10,000", all three measures listed (Loft, Internal wall, Air source heat pump), "What to do next" h2 + installer copy + "Find an approved installer" link, NO back link. (6) Change-link round trip: clicking "Change ownership status" navigates to `/ownership?from=check-answers`, the owner radio is pre-checked from FormContext (proves AD6 + persistence works), selecting `private-renter` then clicking Continue navigates to `/check-answers` (NOT `/income`) — the `?from=check-answers` override fires. (7) Partial/renter result: panel title "You may be partially eligible for a Green Home Grant", body paragraphs match content plan §7 renter copy verbatim. (8) Ineligible/income-too-high: panel class `govuk-panel govuk-panel--not-eligible` (dark grey), body "Your household income is above the threshold for this grant." + always-shown improvement paragraph + "Find other energy efficiency schemes" link. (9) CheckAnswers guard: deep-linking to `/check-answers` on an empty FormContext redirects to `/property-type` via `replace: true` (no back-button loop). (10) Accessibility statement page renders all 8 PSBAR section headings plus the optional "What we are doing to improve" section; back link returns to `/`. (11) GOV.UK wordmark href is `https://www.gov.uk/` (cross-government); service-name uses react-router `<Link>`. (12) Phase banner shows "alpha" tag + en-dash copy. (13) Document title updates on every route change. Console errors during the run are environment-only (HMR WebSocket polling for wss://lab14102.labs.decoded.com/, favicon 404) — no app errors. |
| **What You Changed + Why** | Removed `/// <reference types="vitest" />` from `vite.config.js` in response to an IDE diagnostic ("Cannot find type definition file for 'vitest'"). The reference is a TypeScript-only completion hint; this project is plain JavaScript with no `tsconfig.json`, so the reference adds no runtime value but does generate noise in editors that run loose JS type-checking. Removing it leaves `npm run test:run` still 21/21 passing — vitest reads the `test` block at runtime regardless. No other changes; all reviewer Important/Minor findings from Chunks 1-4 remain skipped as documented in their respective prompts. |

## Prompt 12: [Agent-Jack] Header logotype + details disclosure indicator polish

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Small GOV.UK-conformance improvements to the existing assembled service: (1) replace the plain-text "GOV.UK" wordmark in the header with the canonical Tudor crown logotype SVG and link it correctly; (2) restructure the title bar to the canonical `govuk-header__container` + `govuk-header__logo` + `govuk-header__content` layout; (3) make the `<details>` contextual help (e.g. "Help with annual household income") render the canonical right-pointing triangle disclosure indicator that rotates to down-pointing when opened. Followed the project-level CLAUDE.md / repo-level CLAUDE.md workflow: `git pull --rebase`, advisor() before substantive work, Context7 + WebFetch for verifiable source markup, browser verification at desktop + mobile, tests + build, AI_LOG entry, single commit. |
| **What AI Generated** | (1) `src/components/GovukHeader.jsx` — replaced the plain-text wordmark with the verbatim Tudor crown + GOV.UK logotype SVG from `govuk-frontend/main/packages/govuk-frontend/src/govuk/macros/logo.njk` (primary source via WebFetch, not reconstructed from memory). The SVG includes 8 crown jewel `<circle>` elements, the crown ornament `<path>`, the bullet-dot `<circle class="govuk-logo-dot">`, and the "GOV.UK" letterform `<path>` — combined viewBox `0 0 324 60`, width 162, height 30, `fill="currentcolor"`, `role="img"`, `aria-label="GOV.UK"`, `focusable="false"`, with a `<title>GOV.UK</title>` child for accessible-name purposes. Wrapped the homepage link in `<div class="govuk-header__logo">` and the service-name router `<Link>` in `<div class="govuk-header__content">` per the canonical govuk-frontend template; added `govuk-width-container` to the container class. (2) `src/App.css` — replaced the four `.govuk-details*` rule blocks with the canonical disclosure pattern: `position: relative; padding-left: 25px; list-style: none` on the summary; `::-webkit-details-marker { display: none }` to hide the Safari/Chrome native marker; a custom CSS-border triangle drawn via `.govuk-details__summary::before` (border-width 7/0/7/12.124px, color transparent×3 + currentColor, positioned absolute top:0.4em left:0); `.govuk-details[open] > .govuk-details__summary::before { transform: rotate(90deg) }` to flip the triangle from right- to down-pointing; underline moved from the summary itself to `.govuk-details__summary-text` so it doesn't extend under the triangle; focus state preserved (yellow background + black text + underline removed); `prefers-reduced-motion: reduce` guard nulls the 0.1s rotate transition. (3) `.gitignore` — appended `baseline-*.png` and `after-*.png` patterns alongside the existing `smoke-*.png` rule (verification artefacts at repo root, not source). Verified end-to-end: 21/21 tests still pass, `npm run build` exits 0 (190 kB JS), accessibility snapshot confirms the homepage link's accessible name is now `img "GOV.UK"` (was plain text), Playwright screenshots at 1280×800 and 375×700 show the crown renders correctly and the service-name wraps cleanly next to it on mobile; clicking the disclosure triangle expands the body and rotates the indicator; clicking the "Green Home Grant" service-name link returns to `/` via react-router (FormContext-preserving), the "GOV.UK" wordmark href is `https://www.gov.uk/` (cross-government). |
| **What You Changed + Why** | Two substantive changes from the initial approach, both prompted by `advisor()` consultations. (1) **Source for the crown SVG**: first plan was to use what Context7 returned for `/alphagov/govuk-design-system` — but inspecting the snippet showed 9 small `<path>` elements with `fill="#000"` on a black header, each a tiny diamond/kite in a 3×3 grid. That's an abbreviated docs example, not a real crown — would have rendered as 9 invisible black-on-black shapes. Surfaced the conflict back to advisor; switched to WebFetch on the govuk-frontend `macros/logo.njk` source file to extract the full Tudor crown + logotype path verbatim. The fetched SVG includes the actual crown ornament path (≈900 chars) and the GOV.UK letterform paths — verifiable primary source, not memory reconstruction. (2) **Header restructure scope**: kept it minimal per the user's "small improvements" framing. Added the empty-but-canonical `<div class="govuk-header__logo">` and `<div class="govuk-header__content">` wrappers without adding new CSS rules for them — they work because the existing `.govuk-header__container` flex layout (align-items: center, gap: spacing-3) lays out the two children side by side. **Note for future agents**: the real govuk-frontend stylesheet gives `__logo` ~1/3 width and `__content` ~2/3 width at desktop with the service name dropping onto a new line at narrow viewports; this implementation doesn't ship those rules, so future widening of the service-name area would need explicit CSS. Skipped on purpose — out of scope for "small improvements" and not blocking. Also: clean-up of root-level screenshot artefacts is via `.gitignore` patterns rather than committing or deleting one-by-one, matching the existing `smoke-*.png` convention from commit be4191d. |

## Prompt 13: [Agent-Dale] Initial research — stretch goal "second eligibility pathway (tenant vs. owner routes)"

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | On a new branch (`research/stretch-second-pathway`), perform initial research into the README stretch goal "Add a second eligibility pathway (e.g., tenant vs. owner routes with different questions)" and write it to `docs/research/stretch-routes.md`, presenting options with a recommendation wherever ambiguity arises. No code changes — research artefact only. |
| **What AI Generated** | `docs/research/stretch-routes.md`: a research doc grounded in the actual codebase (read `router.jsx`, `eligibility.js`, `FormContext.jsx`, `QuestionPage.jsx`, `CheckAnswersPage.jsx`, `ResultPage.jsx`, `displayLabels.js`, content plan). Key findings: (1) the app is a single linear 5-question flow; ownership already branches at the *outcome* layer (renter → partial/renter) but **not** at the *question* layer — so the stretch goal = make the questions diverge by tenure. (2) Recommends scope "Option B" — shared prefix (property + ownership) → divergent middle → shared check-answers/result — over outcome-only or two full journeys. (3) Branch on the existing `ownership` field via a shared `isTenant()` helper, not a new question. (4) Recommends one new tenant-only outcome-bearing question (`landlordConsent`), keeping insulation for measures, and flags whether to keep income on the tenant path (ties to the high-income gate). (5) Routing: make `onContinueNavigateTo` accept a function of answers at the branch point (≈3-line change to QuestionPage) rather than a state machine or nested routes. (6) State: one flat object + a path-aware `requiredFields(answers)` helper. (7) Identifies the check-answers/result "redirect if any of 5 fields missing" guards as the highest-risk area — must become path-aware or owners dead-end on the tenant question. (8) Eligibility rules are additive (new "no consent → ineligible" rule above the generic renter rule). (9) GOV.UK angle: branching is endorsed by the Service Manual, but the hard-coded "Step X of 5" indicator becomes factually wrong under branching — recommends removing it (GOV.UK-blessed) or dropping the total. Includes a file-by-file impact map, a 6-step incremental implementation order (infra-first, each step shippable), testing impact, and a consolidated table of 9 open decisions for the user. Sources cited (GOV.UK Question pages pattern, Service Manual form structure). |
| **What You Changed + Why** | Nothing changed from the generated output — accepted as-is. The document is a research artefact with no code to review; its recommendations are explicitly marked as proposals for the user to confirm (§14 decision table), and all GOV.UK claims were verified against primary sources before writing. |

## Prompt 14: [Agent-Dale] Implement the second eligibility pathway (tenant vs. owner routes)

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | On branch `research/stretch-second-pathway`, implement the recommended options from `docs/research/stretch-routes.md` while adhering to PLAN.md constraints: a second eligibility pathway where tenants and owners answer different questions. Branch on the existing `ownership` field; add a tenant-only landlord-permission question; make income/heating owner-only; keep one flat state object with a path-aware required-field list; verify with tests. |
| **What AI Generated** | A full working second pathway. **New files:** `src/flow.js` (central flow definition — shared prefix + owner/tenant tails; exports `flowSteps`, `requiredFields`, `totalSteps`, and the branch-point route helpers `ownershipNext`/`insulationNext`/`insulationBack`); `src/pages/LandlordConsentPage.jsx` (tenant-only Yes/No/Not sure question with hint + help details); `src/__tests__/flow.test.js` (7 tests: owner vs tenant required fields, step counts, branch routing); `src/__tests__/journey.test.jsx` (3 integration tests rendering the real router+context+guards and clicking through the tenant path to partial/renter, the "No" path to no-landlord-consent ineligible, and the owner path to eligible). **Modified:** `eligibility.js` — exported `isTenant()`; hoisted tenure check above the income gate so the high-income exclusion is owner-only, added the `no-landlord-consent` ineligible rule; `FormContext.jsx` — added `landlordConsent` key; `QuestionPage.jsx` — `onContinueNavigateTo`/`backHref`/`totalSteps` now accept a value **or** a function of answers (resolved via a small helper) so the generic page routes owners and tenants differently; `OwnershipPage`/`InsulationPage` wired to the branch helpers; all question pages now take the path-aware `totalSteps`; `router.jsx` — added `/landlord-consent`; `CheckAnswersPage.jsx` — summary list + empty-answer guard + Back link are now path-aware (`flowSteps`), added the "Landlord's permission" row; `ResultPage.jsx` — path-aware completeness guard, new no-landlord-consent copy, indicative measures list in the renter result; `displayLabels.js` — landlord-consent labels; content plan gained a new §11 documenting the question, branched flow, extended rules, result copy, and rationale (copy written into the content plan first, per PLAN.md "do not invent copy"). Result: owners get the original 5-question journey unchanged; tenants get a 4-question journey (property → ownership → landlord consent → insulation). 39/39 tests pass; `npm run build` exits 0. |
| **What You Changed + Why** | Three deviations from settled PLAN.md decisions were required by the approved research and flagged to the user before implementing: (1) **AD12 / §4.4** — the fixed "Step X of 5" indicator becomes factually wrong once the tenant path is 4 steps, so `totalSteps` was made path-aware (kept the indicator, stopped it lying) rather than removed; (2) **§4.3 rule table** — tenure is now checked before the income means-test, making the high-income gate owner-only (research decision #4); all 14 pre-existing eligibility tests still pass unchanged under the new ordering; (3) **§4.1 / §14** — added the `landlordConsent` answer key, and lifted §14's "second eligibility pathway" non-goal under explicit user direction. Browser verification via Playwright MCP (used in Prompt 11) was unavailable this session, so instead of skipping it I added committed full-journey integration tests that render the real components and drive both paths end-to-end through jsdom — stronger and reusable. Decision to keep insulation on the tenant path was made meaningful by surfacing an indicative ("subject to assessment") measures list in the renter result, so no question is collected without being used. |

## Prompt 15: [Agent-Jack] Merge Agent-SK localStorage save-and-return + footer-link polish on top of Agent-Dale tenant pathway

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Review `sk/add_local_storage` (Agent-SK's save-and-return + extended footer + custom crown attempt) and merge it on top of main, which already contained the Tudor crown polish (Prompt 12) and Agent-Dale's tenant pathway (merged in commit 7745752). Preserve our Tudor crown over SK's simpler hand-drawn version; take SK's other additions verbatim; reconcile any cross-branch integration where two branches both modify the same file. |
| **What AI Generated** | Merge commit (`git merge --no-commit --no-ff origin/sk/add_local_storage`) with the following resolution: **conflict (1 file)** — `src/components/GovukHeader.jsx` resolved with `git checkout --ours` to keep the verbatim govuk-frontend Tudor crown + GOV.UK logotype SVG; SK's hand-drawn 5-path crown discarded as user-confirmed inferior. **Auto-merged cleanly (3 files where both branches touched)** — `src/contexts/FormContext.jsx` combines Dale's `landlordConsent: ''` field in INITIAL_ANSWERS with SK's `STORAGE_KEY` / `readPersistedAnswers` / `writePersistedAnswers` / `clearPersistedAnswers` / `resetAnswers` callback (keeping both contributions intact); `src/App.css` keeps both our canonical `::-webkit-details-marker` + `::before` triangle disclosure rules and SK's reorganised non-conflicting blocks; `src/pages/ResultPage.jsx` combines Dale's path-aware `requiredFields(answers)` guard + `no-landlord-consent` reason copy with SK's `resetAnswers`/"Start a new check" call site. **Taken from SK verbatim** — `src/components/GovukFooter.jsx` (now lists Help / Privacy / Cookies / Contact / Terms and conditions / Accessibility statement), `src/pages/StartPage.jsx` (notification banner + "Continue your check" / "Start again" buttons gated on `hasInProgressAnswers`), `src/__tests__/FormContext.test.jsx` (6 persistence tests). **Integration fix required** — SK's `ALLOWED_VALUES` allow-list in `FormContext.jsx` rejects any persisted blob whose keys don't all match `Object.keys(INITIAL_ANSWERS)`, so once Dale's `landlordConsent` field joined INITIAL_ANSWERS the allow-list rejected every restored payload that lacked the new key; two of the six SK tests started failing immediately (`restores answers from localStorage on first mount`, `resetAnswers clears both in-memory state and localStorage`). Fix: added `landlordConsent: ['', 'yes', 'no', 'not-sure']` to `ALLOWED_VALUES` (values cross-checked against `LandlordConsentPage.jsx`) and added the missing `landlordConsent: ''` field to both failing tests' seeded localStorage payloads. **Verification** — 45/45 tests now pass (was 21 pre-merges → 39 after research merge → 45 after SK merge + integration fix); `npm run build` exits 0 (195 kB JS); Playwright at 1280×800 confirms: (1) Tudor crown + GOV•UK logotype + "Green Home Grant" service-name link in header; (2) tenant pathway routes private-renter → `/landlord-consent` with "Step 3 of 4" indicator and our canonical ▶ triangle on the "Why we need to know about landlord permission" help block; (3) extended footer renders Help / Privacy / Cookies / Contact / Terms and conditions / Accessibility statement; (4) `localStorage["ghg:answers:v1"]` populates on first answer and the start page now shows the "You have a partially completed check" notification banner with "Continue your check" + "Start again" buttons on return. .gitignore also extended with `merged-*.png` for this merge's verification artefacts (matching the existing smoke-/baseline-/after-*.png convention). |
| **What You Changed + Why** | Two changes I made beyond the mechanical merge resolution. (1) **Renumbered Dale's AI_LOG prompts 12 + 13 to 13 + 14** during the prior research-merge commit (commit 7745752) because Prompt 12 was already taken by my header-logotype work. Sequence is now gap-free Prompts 1–15 chronologically interleaved across agents. (2) **Added `landlordConsent` to SK's `ALLOWED_VALUES`** rather than the alternative — softening the validation in `readPersistedAnswers` to ignore missing fields. The strict allow-list is load-bearing for SK's "discards malformed data" test and protects against schema-version drift; loosening it would have broken that test and weakened the design intent. Adding the new field to the allow-list is the cheaper change and preserves SK's invariant. Also added the `landlordConsent: ''` seed field to two tests for the same reason: a test that seeds a 5-field shape against a 6-field schema is testing the wrong thing. Browser verification was done after both merges + the integration fix; the dev server is still pointed at the merged tree. SK's prompt entry was never added to AI_LOG by Agent-SK on their branch (-9 lines in the diff, confirmed via `git diff main..origin/sk/add_local_storage -- AI_LOG.md`), so this Prompt 15 entry is the only record of the SK work in the log — readers wanting more detail than the merge commit message should compare `git show origin/sk/add_local_storage` to commit-after-merge to see exactly what came from where. |

## Prompt 16: [Agent-Jack] Fix vite.config.js so the dev server works behind the Decoded lab reverse proxy

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Make the app reachable at `https://app-lab9102.labs.decoded.com/` (proxy in front, never via IP/localhost) by changing configuration only — no app logic. Six requirements: bind `0.0.0.0`; listen on port 5000; accept the lab Host header; assume HTTPS-via-proxy; no hardcoded `http://localhost:PORT` in the frontend; HMR WebSocket over `wss` to port 443. |
| **What AI Generated** | Detected the framework as a Vite 5 + React 18 client-side SPA (no backend server — the only server is the Vite dev server). Rewrote `vite.config.js`: restored the `defineConfig` + `react()` plugin and the `test` (vitest/jsdom) block that the working-tree version had dropped, then set `server.host: '0.0.0.0'`, `server.port: 5000`, `strictPort: true`, `allowedHosts: ['.labs.decoded.com']`, and `hmr: { protocol: 'wss', host: 'app-lab9102.labs.decoded.com', clientPort: 443 }`. Verified locally with the dev server running: `ss` shows `LISTEN 0.0.0.0:5000`; `curl -H "Host: app-lab9102.labs.decoded.com"` returns 200 and serves the real app title; a non-allowlisted host returns 403; grep confirmed `src/` has no hardcoded `http://localhost`/`ws://`/port URLs (only SVG xmlns namespace strings) so requirement #5 needed no change. |
| **What You Changed + Why** | Beyond the literal six asks: (1) restored the `react()` plugin and `test` block the prior working-tree config had silently dropped — without the plugin JSX would not transform and the app/test suite would break; this is configuration, not app logic. (2) Requirement #4 (trust `X-Forwarded-Proto`/`X-Forwarded-For`) is a no-op for a static SPA — there are no server-side redirects, absolute URLs, or `Secure` cookies to fix; the only place the HTTPS-behind-proxy assumption matters is the HMR WebSocket, which the `wss`/`clientPort: 443` config handles. (3) Used `allowedHosts: ['.labs.decoded.com']` (leading-dot subdomain wildcard) rather than pinning the exact host, so the config survives a different lab VM number. Left the unrelated pre-existing `package.json`/`package-lock.json` working-tree changes (html2canvas/jspdf) untouched and out of this commit. |

## Prompt 17: [Agent-Jack] Accessible PDF summary of the eligibility result (stretch goal, PLAN.md §10)

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-03 |
| **Task** | Implement the listed stretch goal "accessible PDF" on a feature branch + PR: let the user download a copy of their eligibility result from the result page, as an *accessible* PDF. |
| **What AI Generated** | A text-based (not screenshot-based) PDF download, split into a pure content model and a thin renderer. **New files:** `src/resultSummary.js` — pure `buildResultSummary(answers)` that derives the document model (headline, £-amount highlight, outcome/reason sections with paragraphs + measure bullets, path-aware answer rows, indicative-result footnote) from the same `eligibility()`/`flowSteps()`/`labelFor()` source of truth as the on-screen `ResultPage`, so screen and PDF never diverge; copy mirrors `ResultPage.jsx` as plain prose (no links/icons, which a PDF can't carry). `src/resultPdf.js` — `buildResultDoc(answers, {date})` (pure, DOM-free, testable) + `downloadResultPdf()` (click-time save). Uses jsPDF text APIs with accessibility baked in: `setLanguage('en-GB')` (WCAG 3.1.1), `viewerPreferences({DisplayDocTitle:true})` so readers announce the title not the filename (2.4.2), document `title`/`subject`/`author` metadata, GOV.UK near-black `#0b0c0c` on white (1.4.3), ≥11pt body / larger headings, single-column top-to-bottom reading order matching the visual order, `splitTextToSize` wrapping with line-by-line pagination, and a descriptive filename `green-home-grant-result.pdf`. **Modified:** `src/pages/ResultPage.jsx` — added a "Download your result (PDF)" primary button in a `govuk-button-group` alongside the existing "Start a new check"; the handler lazy-loads `resultPdf` via dynamic `import()` so jsPDF stays out of the initial bundle. `package.json`/`package-lock.json` — removed the unused direct `html2canvas` dependency (kept `jspdf`); see rationale. **Tests:** `src/__tests__/resultSummary.test.js` (10 tests — every outcome/reason branch, owner-vs-tenant path-aware answer rows, empty-value filtering, undefined-answers robustness) and `src/__tests__/resultPdf.test.js` (6 smoke tests — produces a `data:application/pdf` URI, `/Lang (en-GB)` present, real headline/heading text embedded in the content stream, **no** `/Subtype /Image`, date stamped only when supplied, no throw for tenant/undefined). 61/61 tests pass (was 45). `vite build` exits 0. Verified an actual generated PDF with `pdftotext`: full text extracts in correct reading order with the £ sign and bullets intact, and the file carries `/Lang (en-GB)`, `/DisplayDocTitle true`, `/Title (Your Green Home Grant result)`, and zero image XObjects. |
| **What You Changed + Why** | Three deliberate decisions. (1) **Text, not a screenshot.** The repo's working tree had `html2canvas` pre-added (the usual jsPDF screenshot pairing), but a rasterised DOM capture produces a PDF with no text layer — invisible to screen readers and a WCAG 1.4.5 (Images of Text) failure — which directly contradicts the word "accessible" in the task. I built the PDF from real text instead and **removed `html2canvas` as a direct dependency** (it remains only as jsPDF's own optional dep, lazily referenced by `.html()`, which we never call). Removing a dep another agent had staged is normally something I'd avoid, but it was uncommitted, unused by any code, and actively works against the task's goal; I've flagged it here and in the PR so it can be re-added if wanted. (2) **Split model from renderer** so the wording is unit-testable without a PDF engine and stays derivable from `eligibility()` — avoids the screen result and the PDF drifting apart. (3) **Lazy-loaded jsPDF** via dynamic `import()` after the first build showed it inflating the eager bundle to 592 kB; code-splitting drops the initial chunk to ~196 kB and moves jsPDF into a `resultPdf` chunk fetched only when a user clicks download. Honest limitation recorded: jsPDF does not emit a fully tagged PDF/UA structure tree, so this is "accessible" in the practical screen-reader-legible sense (correct language, selectable text, logical order, contrast, real title) rather than certified PDF/UA — the text-based approach is the prerequisite for either. |

## Prompt 18: [Agent-Jack] Design spec for Playwright E2E stretch goal

| Field | Detail |
|-------|--------|
| **Date + Time** | 2026-06-04 |
| **Task** | Brainstorm and write a design spec for the README stretch goal "Add end-to-end tests with Playwright covering the full user journey", before writing any test code. Followed `superpowers:brainstorming`: explore the codebase, prove the harness with a spike, ask the user the one load-bearing scope decision, propose approaches with a recommendation, then write the spec. |
| **What AI Generated** | `docs/plans/2026-06-04-e2e-playwright-design.md` — a design spec covering: goal/scope (additive test infra only, no app source changes); why E2E earns its keep over the existing jsdom `journey.test.jsx` (reload persistence, real download, focus tree, keyboard, 320px reflow, axe); a **proven** harness section recording spike results (curl shows both `vite dev` and `vite preview` reachable via the lab hostname `lab14102.labs.decoded.com` with no host-check rejection, so no `vite.config.js` server changes needed; Playwright MCP confirmed headless Chromium renders the SPA and focus-moves-to-`<main>`; browsers already cached; `.gitignore` already has a Playwright section); chosen approach (`@playwright/test` driving the **dev** server via Playwright `webServer`, dedicated port 5050, lab hostname baseURL, chromium-only) with rejected alternatives (preview, MCP-interactive) and trade-offs; file layout (`playwright.config.js`, `e2e/helpers.js`, 8 spec files, `e2e/README.md`); a comprehensive coverage matrix — all 6 eligibility outcomes end-to-end, navigation/back/branch routing, check-answers Change round-trip, GOV.UK validation pattern, deep-link guards, localStorage save-and-return across reload, real PDF download event, and an accessibility spec (skip link, focus-on-route-change, keyboard-only step, 320px reflow, a11y-statement nav, plus `@axe-core/playwright` WCAG 2.2 AA scans on five pages); exact option labels mirrored from the page sources; mapping to README acceptance criteria; verification gates (e2e green + 61/61 vitest + build + adversarial review); risks/mitigations; non-goals. Two open decisions were put to the user, who chose: include axe-core scans, chromium-only. APIs verified via Context7 (Playwright `webServer`/`baseURL`/`waitForEvent('download')`; axe `AxeBuilder().withTags().analyze()`/`.exclude()`). |
| **What You Changed + Why** | Design artefact — no code yet. Key decisions made during brainstorming: (1) **dev over preview** — both proved reachable in the spike, but dev is the proven path (Prompt 11), needs no build step, and has no stale-dist footgun; preview's only edge (clean console) is moot since we don't assert console-cleanliness. (2) **lab hostname over localhost** — required by project CLAUDE.md and technically more robust here (`localhost`→IPv6 `::1` would miss the IPv4 `0.0.0.0` bind; the hostname resolves to IPv4). (3) **`*.spec.js` under `e2e/` + narrow Vitest `test.include` to `src/**`** so the two runners never execute each other's files. (4) **No `vite.config.js` server change** — the spike proved the host-check does not block the lab hostname, so the only config edit is the vitest-only `include` narrowing. |
