# Eval/Test Report — Consultation Insights

Generated by running the suite described in `plans/eval-test-plan-agent-tom.md`
against `starter/` (frozen) and today's `solution/analyse.py` (still an
unrefined copy of the original script). Regenerate this file as
`solution/analyse.py` is refined - see Sequencing in the plan.

**Run date:** 2026-07-15. **Suite:** `pytest tests/` from `solution/`.

## Headline result

**14 failed, 14 passed, 1 skipped** (29 collected). Every failure reproduces
a specific, named checklist item; every pass is a specific, verified
checklist item already satisfied. Nothing here is a vague "some tests
fail" - each row below is traceable to `solution/improvement-checklist-agent-tom.md`.

## Baseline (frozen "before" evidence)

`tests/baseline/test_starter_sins.py` — runs against a git-history snapshot
of the *original* `starter/analyse.py` (commit `f7a35f5`), not the live
`starter/` directory, which was patched out-of-band by another contributor
partway through this project and no longer reproduces the original bugs.

| Test | Result | Proves |
|---|---|---|
| `test_malformed_json_crashes_the_whole_run` | PASS | The original script crashes on one bad model response and loses every result computed so far (0 rows saved of 3 attempted). |
| `test_rerun_after_full_success_makes_the_same_api_calls_again` | PASS | Re-running after a full success makes all 4 API calls again, exactly as the README warns. |

These are meant to *stay* passing forever - they document the sin, not test the fix.

## solution/analyse.py — current state (red/green by checklist ID)

### Already satisfied (no code change needed)

| ID | What | Evidence |
|---|---|---|
| R4 | Sustained API failure is reported clearly, bounded retries (library default: `max_retries=2`, confirmed via source inspection) | `tests/system/test_resilience.py::test_permanent_api_failure_is_reported_clearly_not_retried_forever` |
| O1/O2 | README's documented steps produce results.json + a reachable viewer | `tests/system/test_operability.py::test_readme_running_it_steps_produce_results_and_a_reachable_viewer` |
| S5 | Two concurrent runs don't corrupt results.json (full-file overwrite, not partial writes, avoids interleaving in practice) | `tests/system/test_operability.py::test_two_concurrent_runs_do_not_corrupt_results` |
| S2 | API key never appears in stdout/stderr/results.json, even on a crash | `tests/unit/test_security.py::test_api_key_never_appears_in_stdout_stderr_or_results` |
| S3 | Viewer HTML-escapes response-derived text (Flask's `render_template_string` autoescapes by default) | `tests/unit/test_security.py::test_viewer_escapes_response_derived_text_in_html_output` |
| C1 (happy path) | Well-formed JSON is parsed correctly | `tests/unit/test_parsing.py::test_valid_json_response_is_parsed` |
| C2 (format) | The checkpoint write format round-trips correctly | `tests/unit/test_checkpointing.py::test_checkpoint_file_survives_a_round_trip` |
| PII1 | 0% of the real 40-row sample matches an email/UK-phone/NI-number pattern | see PII scan below |

### Not yet satisfied (red - the TDD spec for refinement)

| ID | What's missing | Failing test |
|---|---|---|
| C1 | Non-JSON model output crashes the batch; markdown-fenced/prose-prefixed JSON isn't extracted | `test_malformed_json_does_not_crash_the_batch`, `test_json_wrapped_in_markdown_fence_is_extracted`, `test_json_with_leading_prose_is_extracted`, `test_completely_non_json_response_does_not_raise` |
| R1/R2 | No incremental checkpointing; killing the process mid-run loses everything not yet written | `test_kill_mid_run_then_resume_recovers_completed_rows` |
| C2 | Re-running after a full success re-analyses every row again | `test_rerunning_after_a_full_success_makes_no_new_api_calls` |
| C3 | `themes`/`sentiment` are not validated against the fixed lists | `test_themes_outside_the_fixed_list_are_rejected`, `test_sentiment_outside_the_fixed_enum_is_rejected` |
| S9 | An adversarial response can push out-of-schema output straight through (no schema check to catch it) | `test_prompt_injection_cannot_escape_the_output_schema` |
| S1 | Hardcoded `"PASTE-YOUR-KEY-HERE"` fallback still in source | `test_no_hardcoded_api_key_fallback_in_source` |
| V1 | No per-run cost/token summary in stdout | `test_run_reports_its_own_total_cost` |
| MON2 | No spend cap / kill-switch | `test_run_stops_once_a_spend_cap_is_exceeded` |
| GOV3 | Model referenced by a floating alias (`claude-sonnet-5`), not a pinned dated snapshot | `test_model_string_is_a_pinned_dated_snapshot_not_a_floating_alias` |
| GOV6 | Per-row result has no model-version/raw-output field for later audit | `test_results_retain_enough_provenance_to_answer_why_without_rerunning` |

### Known gap (not yet covered by any test)

- **S4** (CSV/formula injection): explicitly skipped with a documented reason - there is no CSV-export code path anywhere in the current system to test.

## PII scan (checklist PII1)

`python evals/pii_scan.py ../data/responses_sample.csv`:

```
total_rows: 40
rows_with_likely_pii: 0
pct_rows_with_likely_pii: 0.0%
```

None of the 40 real sample rows match an email/UK-mobile/NI-number-shaped
pattern. This is a *screening* result on a 40-row sample, not a guarantee
about the full 1,100/20,000-row consultations - re-run against the full
export before concluding PII redaction (PII4) is unnecessary.

## Quality eval (checklist C4/GOV4) — small real-API validation run

`python evals/run_quality_eval.py --limit 2` (2 of the 18 hand-labelled
golden rows, real API calls, cost logged to `spend/ai-spend-log-Agent-Tom.csv`):

```
n_scored: 2
mean_theme_jaccard: 1.0
sentiment_exact_match_rate: 1.0
mean_summary_word_overlap (LOW CONFIDENCE heuristic): 0.148
real_api_cost_gbp: 0.004
```

Both scored rows matched the hand-labelled themes and sentiment exactly.
**This is a 2-row validation that the script works end-to-end, not a
finding about output quality** - the full 18-row golden set has not been
run against the real API (a deliberate cost decision left for the team to
make; at ~£0.002/row this would cost roughly £0.04 for all 18).

## Cost/scale projection (checklist V3)

`python evals/scale/project_cost.py`, using real measured tokens (389 input
/ 91 output per row, 321 of those input tokens being the repeated,
never-varying `INSTRUCTIONS` block - measured via Anthropic's `count_tokens`
endpoint):

| Rows | Baseline (no caching) | With prompt caching on `INSTRUCTIONS` | Saving |
|---|---|---|---|
| 1,000 | £2.00 | £1.32 | 34% |
| 20,000 | £40.01 | £26.31 | 34% |

Caveats (see `evals/scale/generate_synthetic.py`/`project_cost.py` docstrings):
this is a linear projection from a 2-row real sample, not a measurement at
volume; it doesn't model rate-limit throttling (checklist DEP4, still
uncovered) or Batch API economics (checklist E2, not evaluated).

## CI gate (checklist CI1/CI3)

`.github/workflows/tests.yml` runs `pytest tests/` and a secrets-scan on
every push/PR touching `wk09/**`. Right now, both jobs would show real,
informative failures if run: the test job fails on the 14 items above, and
the secrets-scan job fails on **S1**'s hardcoded key fallback in
`analyse.py`. That's the correct current state, not a broken CI setup.

## Findings outside this suite's scope, surfaced along the way

- `spend/pricing.py` contains a comment claiming `"claude-sonnet-5"` "is not
  a valid model ID" and recommends switching to `claude-sonnet-4-6`. This
  was checked with a real API call during this work and is **incorrect** -
  `claude-sonnet-5` is a valid, callable model. Out of scope to fix here
  (spend/ tooling, not the core pipeline), but flagged so nobody "fixes"
  `analyse.py` to a different model based on that comment.
