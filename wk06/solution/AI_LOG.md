# AI Assistance Log — wk06

This log records the **AI-assistance trajectory** for wk06: for each meaningful
AI-assisted task, what the AI generated, and what the human changed and why. It covers
**both code and doc/process work**, and records only tasks that were **not a one-shot
success** (substantive iteration or a change of direction — not typo fixes). See
`wk06/CLAUDE.md` for the full convention, and the repository-root `CLAUDE.md` for how
this log complements commit messages.

**Entry header:** `## [AgentName] YYYY-MM-DD — <short description>`. No global
sequential numbering — attributed, dated headers let parallel agents on different
machines append in any order without colliding on a shared counter. The four fields
below ("What You Changed + Why" is the important one) are the formal record.

The first entry is a worked example of the format; copy its block for new entries.

---

## [Agent-Jack] 2026-06-24 — Redefine AI_LOG as a trajectory log covering code + docs

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Review the wk06 and repository-root `CLAUDE.md` conventions for the project's audit/decision log and amend them to clarify what the log should capture, given that multiple agents work on different machines in parallel. (A doc/process task, not code.) |
| **What AI Generated** | The earlier agent-authored `CLAUDE.md` files defined each AI_LOG entry with a **global sequential header** (`## Prompt N`, Prompt 0 seeded), scoped the seeded example to **code only** (a triage classification prompt), and left the relationship between commit messages and AI_LOG **implicit**. When first asked which artifact should be *the* audit/decision log, the AI framed it as an **either/or** — commit messages *vs* AI_LOG. |
| **What You Changed + Why** | (1) Rejected the either/or framing: the two are **complementary** — commit messages are the final-state collaboration audit log (git is distributed, attributed, conflict-free on history, so it suits parallel cross-machine work), while AI_LOG is the **trajectory** git can't show (the AI's rejected first draft never becomes a commit). Keeping rationale in both would drift and double the work. (2) **Broadened scope to doc/process changes, not just code** — meaningful iteration on plans, specs, and the `CLAUDE.md`/log files carries the same provenance value. (3) Defined the logging trigger as **"anything not a one-shot success"** with a guardrail (substantive iteration, not typo fixes) to keep the log high-signal. (4) Replaced **global `## Prompt N` numbering** with attributed+dated headers — sequential numbering is a merge-conflict magnet for parallel agents, which defeats the log's collaboration purpose. (5) Put the working log here at `solution/AI_LOG.md`, leaving the `starter/` copy read-only per the wk06 rules. |

---

## [Agent-Tom] 2026-06-24 — Spec tier separation: mvp.md, stretch.md, production.md

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Separate draft specs into three tier-based documents — MVP (gates implementation), Stretch (beyond hackathon rubric), and Production (real deployment) — and mark old agent-prefixed drafts as superseded. |
| **What AI Generated** | Initial draft produced `mvp.md` consolidating `system-architecture-agent-tom.md` and `supervisor-hitl-agent-tom.md`, then `stretch.md` covering S1–S9 from `foi-landscape-synthesis.md`, then `production.md` covering regulatory compliance, security, monitoring, and integration requirements. Schema fixes applied to `AuditEntry` (added `rejection_reason`, `cost_usd`, `triage_topic`, `triage_confidence`), `TriageResult` (added `clarification_recommended`, `clarification_reason`), and `ComplianceResult` (added `third_party_notification_required`). Each stretch goal in `stretch.md` includes an implementation sketch and effort estimate. |
| **What You Changed + Why** | (1) `tooling-agent-tom.md` was not fully superseded — `mvp.md §9` explicitly references it for implementation patterns; status updated to "Active plan — companion to mvp.md" rather than SUPERSEDED. (2) `foi-brief-agent-david.md` header framed as "superseded for implementation purposes" rather than fully superseded — it remains useful as original brief context. (3) `kickoff_prompt.md` stale references (`BaseCallbackHandler`, agent-prefixed naming) corrected in the same commit to avoid new agents picking up wrong patterns. |

---

## [Agent-Tom] 2026-06-24 — Rewrite spec files independently from brief and research

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Rewrite the three agent-tom spec files (mvp-spec, stretch-spec, production-spec) from scratch, sourced only from the authoritative brief (`context/slides/`) and the team's research docs (`docs/research/`), without referencing or citing other agents' documents. The team's working approach is for each agent to produce independent spec/architecture/plan documents for review and consolidation at a later stage. |
| **What AI Generated** | The previous versions of these files had been written after reading Agent-Jack's spec and incorporating its framing. They cited it as a companion document. The initial rewrites used the brief slide text (extracted via HTML parser) and research findings, but retained some structural parallels to the Jack spec. |
| **What You Changed + Why** | (1) Removed all citations to other agents' documents from the three spec files — they now source only from `context/slides/` and `docs/research/`. (2) `mvp-spec-agent-tom.md` restructured around the brief's explicit requirement categories (agents, RAG, HITL, error handling, cost tracking, structured output) rather than the previous implementation-led structure. The "open questions" section retained because they are genuine spec-level unknowns (chunk size validation, confidence score design) that need answering before architecture decisions can be finalised. (3) `stretch-spec-agent-tom.md` separates brief-specified goals (S-B1–S-B4) from research-derived goals (S-R1–S-R4) to make the source clear for consolidation. (4) `production-spec-agent-tom.md` rewritten to focus on requirements framing (what a production deployment must satisfy) rather than implementation options. |

---

## [Agent-Tom] 2026-06-24 — Cost tracking: switch from custom BaseCallbackHandler to built-in get_usage_metadata_callback

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Research LangChain + Anthropic integration patterns before writing the implementation plan, specifically to confirm the correct approach for per-agent token and cost tracking. |
| **What AI Generated** | The initial architecture spec (`agent-tom-system-architecture.md`) and the kickoff prompt both specified writing a custom `BaseCallbackHandler` subclass as `cost_tracker.py` — subclassing it to intercept `on_llm_end` and extract token counts. This was the conventional LangChain pattern from training data. |
| **What You Changed + Why** | Context7 research against the live LangChain docs revealed that LangChain now ships a **built-in** `get_usage_metadata_callback` context manager (and `UsageMetadataCallbackHandler`) in `langchain_core.callbacks`. Using a context manager per agent call is simpler, requires no subclassing, and returns a clean per-model dict with `input_tokens` / `output_tokens` / `total_tokens`. The custom subclass approach was replaced in `docs/plans/tooling-agent-tom.md` and `learning_materials/langchain-callbacks.md`. The architecture spec notes this update pending consolidation. The change reduces `cost_tracker.py` from a callback subclass to a lightweight accumulator that wraps `get_usage_metadata_callback`. |

---

## [Agent-Tom] 2026-06-24 — Restructure docs into specs/architecture/plans

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Restructure the `wk06/docs/` directory from two categories (specs/, plans/) into three — specs/ (requirements only), architecture/ (design decisions and technology choices), and plans/ (all implementation detail) — by creating new files, redistributing content, and deleting the now-consolidated originals. |
| **What AI Generated** | The agent read all seven source documents and produced: `specs/mvp-spec-agent-tom.md` (requirements only, no code), `specs/stretch-spec-agent-tom.md` (S1–S9 requirements only), `architecture/system-design-agent-tom.md` (pipeline topology, supervisor design, HITL principles, error handling, RAG choices, audit trail design, data flow diagram), `architecture/tooling-agent-tom.md` (technology choices table, LLM tiering rationale, LangChain decision, `with_structured_output()` finding, `get_usage_metadata_callback` finding, embedding model and ChromaDB rationale, OpenAI fallback, dependency versions, token cost table), and `plans/implementation-agent-tom.md` (full directory layout, requirements.txt, config.py, .env.example, all Pydantic models with Python code, supervisor call sequence, error handling table, retry/circuit breaker config, HITL display format, interaction flow, audit JSONL example, cost summary format, s.40 prompt text, CostTracker class, JsonFormatter class, testing approach, build order, initialisation checklist). |
| **What You Changed + Why** | The task was structured by the human via a detailed written brief specifying every target file, content mapping, and file operation — so this was largely execution against a clear spec rather than freeform generation. No substantive content corrections were needed. The main agent decisions were: (1) keeping the "Consolidates" and "supersedes" provenance lines in the new architecture/plans files (historical traceability, not stale broken references); (2) leaving `Agent-Jack-SPEC.md` §15 references to `system-architecture-agent-tom.md` and `supervisor-hitl-agent-tom.md` untouched per the task instruction not to modify that file; (3) treating the AI_LOG.md historical entries as immutable records even though they reference old filenames. |

---

## [Agent-Jack] 2026-06-24 — Author the FOI system specification (`Agent-Jack-SPEC.md`)

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Produce an **implementation-agnostic** specification for the wk06 FOI multi-agent system — scope, problem, solution strategy, success criteria — so a context-free agent can understand WHAT the project is and the approach. Driven through a brainstorming dialogue; the implementation plan is deferred to `Agent-Jack-PLAN.md`. (A doc/process task.) |
| **What AI Generated** | A proposed design and then a full first spec draft. The AI's drafts: (a) in *Constraints*, imported **plan-level tooling and `starter/`/lab artifacts** as if they were spec constraints — an OpenAI embeddings fallback, "offline-capable" operation, an in-memory index, and a named orchestration framework; (b) described the gold-answer reference in a way that read like a response *template*; (c) treated `docs/research/` and `kickoff_prompt.md` as authoritative team context, and initially believed the research was its own prior work; (d) proposed `release/partial_release/withhold` as the complete recommendation set; (e) characterised s40 as "absolute in effect" and described s36 as needing only a public-interest test. |
| **What You Changed + Why** | (1) **Stripped the tooling leak**: no OpenAI at all (Claude-only); "offline" is incoherent since Claude is a hosted API; library choices belong in the plan. Pinned only genuine constraints — Claude, ChromaDB (brief-mandated), and a local open-source sentence-transformer <1 GB. (2) **Reframed correctness**: gold answers are an *evaluation yardstick, never emitted* (responses are always generated contextually), plus a **held-out-inputs discipline** — the user tests with requests the builder has not seen — to stop overfitting to the visible corpus. (3) **De-authorised `kickoff_prompt.md`** (a colleague's document, not our rules) — removed it as a source and as context. (4) Chose a **refreshable curated corpus** for live FOIA guidance over runtime scraping, for auditability. (5) Recorded **NCND** as a documented limitation rather than widening scope. (6) After three Sonnet review subagents, applied domain/governance fixes (s40 is *not* absolute; s36 needs the s36(5) qualified-person opinion; bound the audit log to evidence-refs; added an operator-identity floor; committed to AI_LOG ≥3 entries) — but **rejected** the reviewer's "embedding model is a false open question" finding because it conflicted with the user's explicit instruction to benchmark the best <1 GB model. |

---

## [AgentDoubleOSeven] 2026-06-24 — Drive the SPEC/PLAN through an interactive HTML brainstorm planner

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Give the operator meaningful, low-effort input into the FOI system design, then turn that input into `AgentDoubleOSeven-SPEC.md` and `AgentDoubleOSeven-PLAN.md`. Rather than interview the operator turn-by-turn or hand them a finished spec to react to, the AI generated an **interactive HTML brainstorm** (via the `generate-html-planner` skill) — 16 decision cards, each with options + pros/cons + a *recommended default*, plus editable config blocks, model-assignment dropdowns, and per-section notes — with a **Generate-Summary JSON export**. The operator worked through it offline and pasted one JSON object back; the spec and plan were authored **solely** from that JSON (no other agent's documents used). (A doc/process task.) |
| **What AI Generated** | (a) The planner's recommended defaults — including **Opus 4.8 for the compliance agent**, a HITL gate framed around reviewing the **draft response**, **per-request cost shown at the gate**, an **append-only JSONL** audit log, and a proposed triage taxonomy and result-JSON schema. (b) On reading the returned JSON, the AI initially treated the `orchestration: code_supervisor` radio value as settled. |
| **What You Changed + Why** | The JSON export captured five concrete operator overrides that materially shaped the spec, each made by accepting/editing a specific card rather than describing changes in prose: (1) **Downgraded compliance Opus 4.8 → Sonnet 4.6** (operator's dropdown) — cost/quality call the AI would not have made unprompted. (2) **Re-centred the HITL gate on the *decision*, not the draft** ("make sure the decision is center-stage… draft is fine to include") — changed the evidence panel ordering in SPEC §5. (3) **Excluded per-request cost from the gate but mandated it in the logs** (toggle `show_cost:false` + note) — split the cost surface between UI and audit. (4) **Switched the audit log from JSONL to `.txt`** (note) — human-readable audit format. (5) **Deferred the triage taxonomy to the AI** and **required runnable dummy data** (notes) — kept the AI's proposed taxonomy but flagged it provisional, and added Phase-0 dummy requests exercising s40/s12/release paths. Separately, the operator's `notes_orchestration` **contradicted their own radio** — selected plain Python + Anthropic SDK but wrote that they were leaning LangChain and asked the AI to choose. The AI did **not** silently take the radio value: it surfaced the conflict and recommended plain Python + Anthropic SDK (framework abstractions fight the very things the rubric scores — per-call cost capture, the interactive pause, deterministic fallbacks — for a fixed 4-stage pipeline), marking it **confirm-pending** in SPEC §0 so the operator can flip it in one place. The net effect: the HTML planner converted an open-ended "design this" into a set of discrete, attributable decisions the operator could accept, override, or annotate per-item — yielding genuine human authorship of the spec without the operator having to draft it. |

---

## [Agent-Jack] 2026-06-24 — Author + harden the implementation plan (`Agent-Jack-PLAN.md`)

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Turn the spec into a concrete-but-implementation-agnostic plan (architecture + decisions + TDD task outline), grounded by a parallel research sweep, then harden it with review subagents — all scoped to hackathon needs (no added architecture). |
| **What AI Generated** | A research-grounded plan: lightweight `langchain-anthropic` supervisor (no LangGraph), persistent ChromaDB, local embeddings, a citation-verification ladder, gold + held-out eval, 15 TDD tasks + stretch. The first draft (a) picked `gte-base` embeddings on the rationale it was "drop-in, no remote code"; (b) ordered the pipeline `triage→compliance→redaction→response`; (c) left interface gaps (stub-LLM injection point, `case.retrieved` assignment site, `modify`→final-output wiring, evidence-refs in the audit entry, per-call vs per-stage cost); (d) had no prompt-injection consideration. |
| **What You Changed + Why** | (1) **Embedding:** a review (Context7) showed `gte-base` *also* needs `trust_remote_code` — the "drop-in" rationale was wrong — so you chose the higher-quality **`nomic-embed-text-v1.5`**, accepting a small prefix wrapper; indexing/retrieval rewritten to embed explicitly with `search_document:`/`search_query:` prefixes (a collection EF can't apply different prefixes for docs vs queries). (2) **Pipeline-order bug:** redaction must act on the *drafted* response, so reordered to `triage→compliance→response→redaction` in **both** plan and spec (the spec's §5 diagram and §6.4 were self-contradictory). (3) **Closed interface gaps** the reviewers found (stub-LLM monkeypatch target; supervisor sets `case.retrieved`; `modify` override becomes the final letter; decision audit entry carries `evidence_refs`; `s41` recognised; cost emitted per-call; cosine-on-reopen guard). (4) **Prompt-injection hygiene** (delimit request text as data; HITL + grounding as backstops) + one regression test, and a **cost/latency estimate** — both kept strictly in-scope (no new architecture), per your "don't expand complexity" steer. |

---

## [AgentDoubleOSeven] 2026-06-24 — Re-altitude the spec to implementation-agnostic + sync the plan's ACs

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Tighten the spec/plan split after the first drafts: redesign the spec to be **implementation-agnostic** and add acceptance criteria + tests, then bring the plan's acceptance criteria **into sync** with the spec's and add explicit unit-testing and human-review stages. (A doc/process task.) |
| **What AI Generated** | The first `AgentDoubleOSeven-SPEC.md` **mixed altitudes** — it embedded module/file layout, full Pydantic schema shapes, concrete model IDs, retrieval depth (`k`), the section-aware chunking strategy, and prompt-level detail directly in the specification. The companion `AgentDoubleOSeven-PLAN.md` carried its **own independent acceptance-criteria numbering** (`AC-0.1`…`AC-11.3`) disconnected from the spec, cross-referenced spec sections (`SPEC §2.2/§3/§4/§8/§9`) for implementation detail, and folded testing implicitly into a `Lint → Human review → Commit` checkpoint. |
| **What You Changed + Why** | The operator supplied a precise **definition of "spec"** — *what/why/approach only; no file layout, function signatures, schema shapes, prompt text, chunk sizes, retrieval depth, or model IDs; readable standalone by a context-free agent* — and required (a) acceptance criteria + tests **in the spec**, (b) the plan's ACs **in sync** with the spec's, and (c) explicit **unit-testing and human-review stages** in the plan. Applying that: (1) **Stripped every implementation detail out of the spec** (layout, schemas, model IDs, `k`, chunk sizes, prompts) and relocated it to the plan, leaving the spec as intent + constraints (the *givens* — Claude, ChromaDB, CLI — separated from our solution strategy) + an 11-point approach. (2) **Added a testable contract to the spec**: 25 capability-level acceptance criteria (`AC-A1`…`AC-K1`), a behavioural, code-free **test specification** (`T-*` scenario tables mapped to ACs), a test-data strategy with a **held-out generalisation** requirement, and a **definition of done**. (3) **Made the plan realise the spec**: re-pointed every phase at the spec's `AC-*`/`T-*` IDs (retiring the parallel `AC-0.1` scheme), added a dedicated **Unit tests** stage per phase, changed every checkpoint to **Lint & type-check → Unit tests pass → Human review → Commit**, and replaced the now-stale `SPEC §x` cross-references with inline implementation detail so the plan self-owns the *how*. A phase→AC→test coverage table proves every spec criterion and test is realised. Why: the spec becomes the implementation-independent acceptance contract (any builder can verify against it), and the plan becomes the verifiable build that satisfies it — one shared AC vocabulary across both, with testing and human sign-off made explicit gates rather than implicit habits. |

---

## [Agent-Collator] 2026-06-24 — Collate four independent FOI plans into one ratified PLAN.md

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | As a fresh, unbiased agent, consolidate four intentionally-divergent implementation plans (Agent-Jack, Agent-Tom, AgentDoubleOSeven, Agent-David) into a single best `PLAN.md` — deciding per-dimension, choosing one coherent spine, and grafting only compatible best-ideas. Two-phase: produce a ratified decision record first, then write the plan. (A doc/process task.) |
| **What AI Generated** | A multi-agent collation **workflow** (4 equal-footing Opus advocates → Opus judge → adversarial Opus critic → Opus reconcile; ~855k tokens) produced a per-dimension decision matrix. It recommended **Agent-Jack as the trust-critical core** (RAG correctness, citation-grounding ladder, schemas, per-call cost, injection hygiene, TDD tasks, held-out eval) on a shared **langchain-anthropic** spine, with **AgentDoubleOSeven** grafts (single-seam principle, section-aware chunking, five-layer reliability, hybrid redaction, phased checkpoints) and **Agent-Tom** grafts (rendered HITL display, circuit breaker, CostTracker polish, confidence/Modification/third-party schema fields), plus **Agent-David**'s two audit governance defaults and MVP fallback. The critic passed it with three graft-incoherences and three coverage notes, which the reconcile pass folded in. The workflow marked the spine dimension as **settled** (`close_call=false`). |
| **What You Changed + Why** | (1) **Re-opened the spine as a genuine close-call** the workflow had closed: I independently read all four plans + the authoritative brief and noticed the spine is convergent for only *three* of four plans — **AgentDoubleOSeven deliberately uses the plain `anthropic` SDK**, and both the **starter scaffold** (plain-Python `CostTracker`) and `wk06/CLAUDE.md` (names "the `anthropic` SDK") lean plain-SDK. I surfaced this to the human rather than letting the majority silently win; they **ratified LangChain**. (2) **Reframed the team-size question:** the human clarified the "team" is *a single Claude Opus 4.8 agent overnight*, so I resolved dims 2 & 11 for that model — Jack's full package layout **+ Jack's 15 linear named-test TDD tasks** (most executable sequential hand-off) wrapped in **007's per-task checkpoint discipline** — instead of 007's team-parallel phasing (whose payoff is moot for one agent). (3) **Context7-verified the load-bearing APIs** before writing concrete code: confirmed `with_structured_output(method="json_schema")` requires `langchain-anthropic>=1.1.0`, and **replaced Jack's manual `search_document:`/`search_query:` prefix concatenation with `encode_query`/`encode_document`** (modern sentence-transformers auto-applies nomic's prompts) — a correctness improvement the source plans predated. (4) Ratified the remaining close-calls on the recommended defaults (nomic w/ MiniLM fallback; all four stretch goals w/ redaction first-to-cut; cost hidden at the gate; strict CLI operator identity; single `.with_retry()` retry dropping tenacity; stated eval-coverage boundary). Why: the brief demanded judging ideas on merit not document length (~5× spread) and flagging genuine close-calls for humans — so the value I added beyond the workflow was catching the one over-confident decision, grounding the riskiest APIs against current docs, and tailoring the build process to the actual (single-agent) executor. No per-agent plan was deleted; each winning dimension is attributed in both `PLAN.md` and `COLLATION-DECISION-RECORD.md`. |

## [Agent-Jack] 2026-06-24 — Task 1: scaffold + config + schemas (env-setup iteration)

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | Bootstrap the `foi_system/` package per PLAN.md: `pyproject.toml`, `config.py` (model tiers, verified Claude price table, paths, thresholds), `models.py` (all 13 Pydantic v2 schemas — the shared interfaces every later task depends on), and `tests/test_models.py` (5 named validation tests). TDD red→green. |
| **What AI Generated** | The schema/config implementation itself was a one-shot success (all 5 checkpoint gates — ruff, ruff format, mypy, pytest, import smoke — green on first report; reviewer confirmed spec ✅ + quality approved). The *meaningful iteration* was the environment bootstrap: the first `pip install` of the dependency set let `sentence-transformers`/`torch` resolve to the **default CUDA build**, pulling ~1.9 GB of `nvidia-cu13-*` + `triton` wheels onto a 32 GB `/home` that was already ~30 GB full — the install died with `OSError: [Errno 28] No space left on device`, leaving a broken 2.4 GB venv and a 2.8 GB pip cache. |
| **What You Changed + Why** | (1) Diagnosed via `df -h` that `/home` was at 100%, with `nvidia/` (1.2 GB) + `triton/` (691 MB) the culprits — GPU wheels we never use (embeddings run CPU-only). (2) Purged the pip cache and deleted the half-written venv (freed 5.6 GB → 5.1 GB free). (3) Reinstalled **CPU-only torch** first via `--index-url https://download.pytorch.org/whl/cpu` (`torch-2.12.1+cpu`, ~190 MB) so `sentence-transformers` saw torch already satisfied and skipped the CUDA pull, with `--no-cache-dir` so the cache couldn't re-fill the disk. Result: clean install, all imports OK, `/home` at 89% (3.4 GB free — enough headroom for the Task-3 nomic model download). (4) Build-time pins captured: `langchain-anthropic` installed **1.4.7** (≥1.1.0 floor for `with_structured_output(method="json_schema")` holds); Claude price table verified against current pricing (Haiku $1/$5, Sonnet $3/$15 per MTok). Also took the implementer's one deviation — PEP 517 backend `setuptools.build_meta` instead of the brief's stale `setuptools.backends.legacy:build` — which the reviewer confirmed is the correct standard backend. Why it matters: the CPU-torch pivot is invisible in the committed code (the venv is gitignored) but is the load-bearing reason the build is reproducible on this disk-constrained lab box; it lives nowhere but here. |

## [Agent-Jack] 2026-06-24 — Task 2: LLM single-seam + cost tracker (composition-order fix)

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | `foi_system/llm.py` (the single seam: `build_llm` + `structured`) and `foi_system/cost.py` (`CostTracker`, per-call `CostEntry` emission via `get_usage_metadata_callback`), with offline `tests/test_cost.py`. TDD red→green. |
| **What AI Generated** | PLAN.md §3.2 sketched the seam as `build_llm(...).with_retry(...)` returning a `RunnableRetry`, with `structured()` then calling `.with_structured_output(...)` on it. The cost design and tests were straightforward, but that seam sketch carries a latent bug. |
| **What You Changed + Why** | Verified the plan's load-bearing langchain APIs against the **actually installed** packages (langchain-anthropic 1.4.7 / langchain-core 1.4.8, both newer than the plan's Context7-confirmed 1.1.0) before writing code: `get_usage_metadata_callback` is in `langchain_core.callbacks` with a `.usage_metadata` dict keyed by model name; `with_structured_output(schema, method="json_schema")` is supported; `.with_retry(...)` is on Runnables. Crucially, I caught that the plan's order **fails** — `.with_retry()` returns a `RunnableRetry`, which has **no** `with_structured_output` method (confirmed by reproducing the `AttributeError`). Corrected to the verified composition: `structured()` applies `with_structured_output(schema, method="json_schema")` to the bare `ChatAnthropic` **first**, then wraps with `.with_retry(stop_after_attempt=4, wait_exponential_jitter=True)`; `build_llm` returns the bare model with `max_retries=0`. This preserves both ratified invariants — the single seam (every model call still routes through `structured()`) and the single retry mechanism (one `.with_retry()`, no SDK retry, no tenacity) — while actually building. Minimal call consistent with the decision record; no scope added. Also accepted two scoped `# type: ignore` lines for the langchain-anthropic Pydantic-model mypy-stub gap (stubs declare `__init__(*args, **kwargs)`), confirmed legitimate by the reviewer. |

## [Agent-Jack] 2026-06-24 — Task 3: indexing + Day-1 nomic validation gate

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | `foi_system/indexing.py` — section-aware chunking, nomic `encode_query`/`encode_document` embeddings, persistent ChromaDB (cosine, verify-or-recreate guard), freshness check — plus synthetic fixtures and 6 tests. Includes the PLAN.md §8 **Day-1 embed-download validation gate**. TDD red→green. |
| **What AI Generated** | First the controller ran the Day-1 gate: loaded `nomic-ai/nomic-embed-text-v1.5` (`trust_remote_code=True`) in the lab env — it succeeded (dim 768; `encode_query`/`encode_document` present; cosine(s40-query, s40-doc)=0.92), so **nomic is used, the MiniLM fallback is NOT needed**. Then the implementer's first section-heading regex used `re.IGNORECASE`. |
| **What You Changed + Why** | (1) **Verified the chromadb 1.5.9 + nomic APIs against the installed packages** before writing the brief, not from memory — confirmed the cosine config form `configuration={"hnsw":{"space":"cosine"}}`, that the space is read back on reopen via `collection.configuration_json["hnsw"]["space"]` (NOT `collection.metadata`, which is `None`), that int-epoch `where` filters work, and that passing our own embeddings bypasses chroma's default ONNX EF. These exact facts went into the brief so the implementer didn't guess a version-specific API. (2) **TDD caught a real correctness bug in the trust-critical chunker:** `re.IGNORECASE` made the heading regex match body-text mentions like "Section 40 provides…", splitting each statutory section into two chunks and breaking the "one exemption == one citable chunk" invariant that citation grounding depends on. The failing `test_section_aware_chunk_is_one_exemption` surfaced it; the fix was to make the regex case-sensitive/anchored (`^SECTION\s+(\d+)\b`) so only real uppercase headings trigger a split. This is exactly why the chunker is built test-first: a silent over-split would have degraded retrieval precision and citation grounding with no error. (3) Added `path`/`name`/`now` parameters (config-defaulted) so the persistent store is testable against a tmp ChromaDB and freshness is deterministic, without changing production call sites. |

## [Agent-Jack] 2026-06-24 — Task 5: triage agent + the agent unit-test seam (deviation from "FakeListChatModel")

| Field | Detail |
|-------|--------|
| **Date** | 2026-06-24 |
| **Task** | `foi_system/agents/triage.py` — classify topic/complexity/summary/confidence, duty-to-assist clarification, prompt-injection hygiene, typed fail-safe fallback — with offline `tests/test_triage.py`. TDD red→green. First of four agent tasks (5/7/8/9). |
| **What AI Generated** | PLAN/decision-record §10 specified **`FakeListChatModel`** as "the standard unit seam" for agent tests (a Tom/David graft). The triage logic itself (prompt, empty guard, fallback) was straightforward. |
| **What You Changed + Why** | Verified the test seam against installed langchain BEFORE writing the brief and found the ratified approach **does not work**: `FakeListChatModel.with_structured_output(...)` raises `NotImplementedError` (it's a generic fake with no structured-output support), so a fake chat model cannot drive our `structured()` path at all. Rather than abandon unit-testability, I changed the seam to **dependency injection of the already-structured runnable**: every agent takes a keyword-only `llm: Runnable | None = None` defaulting to `structured(build_llm("<agent>"), <Schema>)` (the production single-seam path is unchanged), and tests inject a `RunnableLambda` that returns the Pydantic result (success) or raises (to exercise the typed fallback). This preserves both ratified invariants — the single seam (production still routes through `structured(build_llm(...))`) and offline unit tests with no network — while actually running. Minimal call consistent with the decision record's *intent* (offline fake-driven agent tests); the named-test set is unchanged. This pattern will be reused for compliance/redaction/response (Tasks 7–9). Also added a deterministic empty/whitespace-request guard that returns a clarification result without any model call (duty to assist, and a clean offline test of that path), and confirmed the `<foi_request>…</foi_request>` untrusted-data delimiter + SECURITY system instruction are present (the injection regression test proper lands in Task 7's compliance agent). |
